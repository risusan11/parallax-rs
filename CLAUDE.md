# PARALLAX-RS

2〜4人用・2D協力型・数学謎解き脱出ゲーム。
各プレイヤーは同じマップを歩くが「観測層」が異なり、情報を組み合わせないと数学的構造が見えない。
数学問題を画面に表示するのではなく、ゲーム世界そのものを数学問題にする。

## 技術スタック

- TypeScript (strict)、npm workspaces によるモノレポ
- `packages/core` — 純粋ロジック(数学・判定・ステージ定義)。DOMもPhaserも参照しない
- `packages/server` — Colyseus ルームサーバー
- `packages/client` — Phaser 3 クライアント(Vite)
- テスト: Vitest / Lint: ESLint + Prettier

## コマンド

- `npm test` — 全パッケージのテスト実行(PR前に必ず全部通すこと)
- `npm run lint` — Lint
- `npm run build` — 全パッケージのビルド

## アーキテクチャ原則(最重要)

1. **世界の状態はサーバーが持つ唯一の真実。** クライアントは受信した状態を描画するだけ
2. **役職とステージはコードではなくデータ。** 役職 = 可視レイヤーのフィルタ + 使用可能ツールの集合。
   ステージ = 条件セット + レイヤー割当のデータ定義。新ステージ・新役職の追加が
   `packages/core/src/stages/` へのデータ追加とテストだけで済む構造を維持する
3. **数学ロジックはすべて core に置き、純粋関数として書く。** 例: 直線と点の距離、
   合同条件の充足判定、順列条件のフィルタ。必ず単体テストを付ける
4. 同期は低頻度で成立するパズル主体の設計。tick単位の精密同期を要求する実装をしない

## 開発ルール

- ブランチは `claude/` プレフィックスで作成。main への直接 push 禁止
- テストが通らない状態で PR を出さない
- 1つのPRは1つのROADMAPタスクに対応させる
- PR本文は日本語で「何を・なぜ・どう確認したか」を書く
- ROADMAP.md にないスコープの機能追加・リファクタ・依存追加をしない
- 新しい npm 依存の追加は原則避ける。必要な場合は PR 本文に理由を明記する
- `any` 禁止。型で表現できるものは型で表現する

## 独断で進めてよい範囲 / 確認が必要な範囲

- OK: core のロジック実装、テスト追加、データ定義追加、バグ修正、タスク内のファイル構成判断
- 要確認(PRコメントで質問を残して人間の判断を待つ): ゲームデザインの変更、
  アーキテクチャ原則に反する変更、依存の大きな追加、ROADMAPタスク自体の解釈が
  複数ありうる場合
