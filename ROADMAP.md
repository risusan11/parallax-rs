# ROADMAP

目標(縦切り第1弾): ステージ「見えない座標室」を観測者+測量士の2人版で、
ロビー入室からクリアまで通しプレイできる状態にする。

ルール: 上から順に1タスクずつ。各タスクは1回の自律実行で完了する粒度で書く。
完了したら `- [x]` に更新する。

## フェーズ0: 土台

- [x] npm workspaces モノレポの雛形を作る(core/server/client の3パッケージ、
      TypeScript strict、Vitest、ESLint+Prettier)。core にダミー関数と
      テスト1件を置き、ルートの `npm test` と `npm run lint` が通ること
- [ ] core: 2Dベクトルユーティリティ(加減、距離、線分交差)と、
      直線(y=ax+b および x=c)と点の距離・直線上判定を実装しテストを書く
- [ ] core: ステージ定義スキーマを設計する。レイヤー(タイル・オブジェクト・
      テキスト条件)、役職(可視レイヤー + ツール)、クリア条件をデータで表現する
      型と、定義のバリデーション関数 + テスト
- [ ] core: 「見えない座標室」のステージデータを定義する(部屋レイヤー、
      座標軸+障害物レイヤー、条件テキストレイヤー、クリア条件:
      「直線 y=2x-3 上かつ装置から距離5 の点に観測者が到達」)。
      候補点の算出関数と到達判定関数 + テスト

## フェーズ1: サーバー

- [ ] server: Colyseus のルーム作成・入室・退室を実装する。入室順に
      役職(観測者→測量士)を割り当て、ルーム状態に反映する。ルームIDでの
      合流をテストで検証する
- [ ] server: 観測者の移動入力を受けて位置を更新し、全クライアントに
      同期する。障害物レイヤーとの衝突判定はサーバー側で行う
- [ ] server: ステージ状態機械(待機→プレイ中→クリア)を実装する。
      クリア判定は core の到達判定関数をサーバー側で呼ぶ

## フェーズ2: クライアント

- [ ] client: Phaser + Vite の雛形。サーバーに接続し、ルーム状態を
      コンソールに表示できるところまで
- [ ] client: タイルマップ描画と観測者の移動操作(矢印キー/WASD)
- [ ] client: 役職別レンダリングフィルタを実装する。観測者には部屋と自分のみ、
      測量士には座標軸・障害物・観測者の位置マーカーのみが見えること
- [ ] client: 条件テキストレイヤーの表示(測量士に割当)と、クリア演出・
      「クリア」状態の全員への表示
- [ ] client: ロビー画面(ルーム作成 → URL共有 → 合流 → 2人揃ったら開始)

## フェーズ3: 縦切りの仕上げ

- [ ] リザルト画面(クリア時間・観測者の移動距離・リトライ回数)
- [ ] デプロイ設定(クライアント: 静的ホスティング / サーバー: Node ホスティング)。
      手順を README に書く

## 以降(縦切り完了後に人間と相談して追加)

- 解読者役(3人目)と表現形式の分割
- ステージ2以降のデータ追加
- 調停者役と情報信頼性システム
- ストーリーログ断片(層別テキスト)
