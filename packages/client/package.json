{
  "name": "@parallax-rs/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "@colyseus/sdk": "^0.17.43",
    "@parallax-rs/core": "^0.0.0",
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
